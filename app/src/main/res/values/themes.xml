<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DineVibes" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/dark_gray</item>
        <item name="colorPrimaryVariant">@color/dark_gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- status bar icons color   -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/white</item>

        <!-- Custom button style -->
        <item name="buttonStyle">@style/Widget.DineVibes.Button</item>
    </style>

    <style name="Theme.DineVibes" parent="Base.Theme.DineVibes" />

    <!-- Custom button style -->
    <style name="Widget.DineVibes.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/button_background</item>
        <item name="strokeColor">@color/button_border</item>
        <item name="strokeWidth">1dp</item> <!-- Adjust the width as needed -->
        <!--   Corner radius     -->
        <item name="cornerRadius">8dp</item> <!-- Adjust the radius as needed -->
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
